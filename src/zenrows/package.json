{
	"name": "@modelcontextprotocol/server-zenrows",
	"version": "0.1.0",
	"description": "MCP server that fetches webpages via ZenRows with cost‑aware escalation",
	"license": "MIT",
	"type": "module",
	"bin": {
		"mcp-server-zenrows": "dist/index.js"
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsc && shx chmod +x dist/*.js",
		"prepare": "npm run build",
		"watch": "tsc --watch"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^0.5.0",
		"zenrows": "^2.0.1"
	},
	"devDependencies": {
		"@types/node": "^22",
		"shx": "^0.3.4",
		"typescript": "^5.3.3"
	}
}
